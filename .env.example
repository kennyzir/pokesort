# Server/public Function metadata (not secrets)
DAILY_ENVIRONMENT=preview
DAILY_API_ENABLED=false
# DAILY_ENVELOPE_HMAC_KEY is an encrypted Pages secret; do not put its value here.

# CI-only admin upload inputs. Store real values as encrypted CI secrets.
CLOUDFLARE_ACCOUNT_ID=
POKESORT_PREVIEW_DAILY_KV_NAMESPACE_ID=
POKESORT_PRODUCTION_DAILY_KV_NAMESPACE_ID=
CLOUDFLARE_DAILY_KV_API_TOKEN=example-encrypted-ci-secret
POKESORT_PREVIEW_DAILY_ENVELOPE_HMAC_KEY=example-preview-encrypted-ci-secret
POKESORT_PRODUCTION_DAILY_ENVELOPE_HMAC_KEY=example-production-encrypted-ci-secret
