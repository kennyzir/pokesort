# PokeSort SEO Roadmap Execution Ledger

## Question-bank remediation controller — started 2026-08-24

Objective: repair the local question-bank candidate so future Daily content remains private, UTC activation is atomic, Daily/Infinite quality claims are measurable, manifest semantics are independently verified, and the production build fails closed without reducing existing product or SEO surfaces.

Authoritative roadmap: `docs/roadmap/QUESTION_BANK_REMEDIATION_ROADMAP.md`, approved by the user in this Codex task on 2026-08-24.

### Protected baseline and ownership

- Baseline commit: `87e5ea9a164881b071652e7119ff356c2e4d8d35` (`main`, aligned with `origin/main` at controller start).
- The worktree is intentionally dirty with the completed local question-bank candidate, Pokelike Today HOLD implementation, GA4/privacy/SEO changes, and their documentation. These changes are preserved as protected inputs; this controller will use narrow patches and will not reset, clean, bulk overwrite, or silently commit them.
- Protected product baseline: 9 evergreen/indexable source routes expanded locally to 40 indexable routes with 31 elapsed Daily pages; latest-31 Archive; homepage with 3 major sections and 9 game controls; Daily/Infinite/Archive; local progress/streak/share; hints/reveal/failure; keyboard/mobile behavior; Pokelike guide and Today HOLD; redirects; true 404; canonicals, sitemap, schemas, analytics privacy, and responsive layout.
- Protected data baseline: 1,025 pinned Pokémon facts, 100 canonical rule instances, 365 generated Daily manifests (31 elapsed and 334 future as of 2026-08-24), and 1,000 Infinite manifests. The future calendar and fixed public seed are unsafe publication candidates and must not be staged or released.
- Repository-local implementation, tests, task coordination, and integration were authorized by Roadmap approval. The user separately authorized commit, push, and deployment in this task on 2026-08-24. Target-account Cloudflare resource/binding/secret mutation remains technically blocked by the verified account mismatch; GSC actions, paid services, and unrelated external communication remain outside scope.

### Remediation phase ledger

| Phase | Status | Reviewed/integrated scope | Verified evidence | Inferred / unverified | Blockers / rerun |
|---|---|---|---|---|---|
| R0 — Publication freeze and baseline | PASS — 2026-08-24 | Froze the unsafe fixed-seed calendar from publication, ignored all local Daily candidate manifests, and added a tracked/staged/explicit-candidate leak Gate covering future dates, indexes, known seed markers, and production-source literals without deleting recoverable local data. Recorded the 31 elapsed/334 future split and protected surfaces. | `npm run test:publication-leaks` PASS for positive, negative, staged, untracked, index, seed, and explicit candidates; `npm run check:publication-leaks` PASS on the current tracked set; `git ls-files data/puzzles/daily` is empty; full `npm test` and `npm audit --audit-level=low` PASS; 40 local routes and protected tasks preserved. | Local ignored future artifacts remain private worktree inputs, not production evidence. CI/Cloudflare cannot yet build the candidate because public elapsed history and private future delivery are not separated. | R1 may proceed. Do not stage `data/puzzles/daily/*.json`; production release remains blocked until R1–R7 and the exact deploy candidate build without ignored inputs. |
| R1 — Canonical semantic verifier/build Gate | PASS — 2026-08-24 | Added one canonical verifier backed by the pinned facts and full 100-rule universe across Daily, Infinite, optional manifest inputs, and emitted shards. It recomputes exact Pokémon identity, predicate membership, labels, hints, explanations, source/boundary/provenance, matching-rule evidence, dataset/model versions, hashes, index receipts, and unique partitions. Infinite was reproducibly enriched with the same evidence schema. `npm run build` now runs the production-data Gate before removing or emitting `dist`. Main review found five first-pass bypasses—optional identity fields, unbound dataset/model, skip-solver semantic bypass, self-declared calendar range, and a non-executed shard fixture—and returned them to the same stage for narrow repair before acceptance. | `validate:production-data` PASS for 365 Daily/1,000 Infinite; canonical Gate rejects fabricated predicate/copy/source/boundary/member/evidence. Independent R1 exploit regression rejects deleted identity fields, fabricated facts/dataset/model, duplicate shard names, fully rehashed puzzle→shard→index tampering, altered Daily receipts, and self-shortened calendars. `--skip-solver` still checks semantics. Failed production builds preserve a pre-existing output sentinel; two clean 91-file builds are byte-identical. Full `npm test` and Chrome 390×844 PASS with 40 indexable routes; `npm audit` 0 vulnerabilities; `git diff --check` line-ending notices only. | CI still lacks the ignored Daily candidates by design; public elapsed history/private future delivery are R2/R4 concerns. Candidate scripts/data remain an untracked protected release unit, not production evidence. | No R1 blocker. R2 and R3 may proceed; do not weaken the pre-emission Gate or stage ignored future manifests. |
| R2 — Daily quality/private buffer | PASS — 2026-08-24 | Added a versioned Daily quality policy, non-trivial three-card-overlap measurement, bounded quota-aware difficulty generation, seed-free manifest hashing, private 30-day buffer preparation/validation, immutable elapsed-byte receipts, rolling history-to-private cooldown checks, quality/calibration reporting, and factual-overlap browser feedback. Added one source-backed exact-monotype Bug predicate (23 pinned species, 8,855 quartets) to raise strict year-capable predicates from 59 to 60 without weakening the 14-day cooldown or changing elapsed content. The R2/R4 integration correction added remaining-capacity-aware rule scheduling after reciprocal Infinite exclusions exposed late-year exhaustion; it preserves all original thresholds and bounded search. | All 31 elapsed manifest SHA-256 values and their quartet/partition/intended evidence remain unchanged. The final deterministic 365-day stress runs twice through the real private-buffer path while excluding 1,000 Infinite boards and 4,000 exact/member groups: 365 accepted, zero board/exact/member collisions, valid quartets 14–100, three-card overlap 0–30, difficulty 100 easy/185 medium/80 hard/0 extreme, bounded derivation 5/8, stable receipt `985a874916…cca02`. The active ignored 30-day buffer (`2026-08-25..2026-09-23`) passes R1 semantics, quality, cooldown, seed-leak, elapsed-byte, and reciprocal-Infinite exclusion Gates with receipt `b2f6f1eae6…3267e`. Full `npm test`, Chrome 390×844 task matrix, leak scan, audit, and diff check PASS. | Private buffer remains local ignored evidence, not Cloudflare production data. | No R2 blocker. Preserve capacity-aware scheduling and the reciprocal-exclusion stress Gate; do not replace it with a generator-only test. |
| R3 — Infinite diversity/copy integrity | PASS — 2026-08-24 | Replaced the narrow anchor selection with a capacity-aware scheduler across all eight advertised families, versioned the 101-rule category model and Infinite schema, rebuilt 1,000 immutable boards/20 shards, added species/pair/family exposure accounting, and bound public capability copy to the measured PASS report. Main review found that the first validator recomputed exposure maps but trusted the stored diversity summary; the correction loop now recomputes the canonical summary from the actual 1,000 boards/4,000 groups and requires both report and index summaries to match it. | Full canonical validation PASS: 1,000 unique boards, 4,000 unique exact/member groups, all 365 Daily boards excluded, all 8 families covered, maximum family share 20%, 1,025/1,025 species covered, maximum 41 boards/species and 9 boards/pair, first 500 runtime selections unique, byte-stable rerun, tamper rejection, and full `npm test` browser regression PASS. Independent adversarial regression rejects a forged summary even after report/index hashes are recomputed; public capability copy consumes only the fresh validator result. | Production cache behavior remains an R8 field check. | No R3 blocker. Preserve schema-v2 validation, fresh-summary recomputation, and capability-copy fail-closed behavior. |
| R4 — Private storage/UTC edge delivery | LOCAL PASS / EXTERNAL CONDITIONAL — 2026-08-24 | Added date-addressed HMAC-SHA-256 v2 KV envelopes, `/api/daily/current` and `/api/daily/[date]` contracts, server-authoritative UTC selection, upload-time canonical R1 verification, edge authentication/hash checks, preview/production key isolation, explicit-path admin upload, redacted logging, payload limits, and exact seven-day preload enforcement. The upload receipt states the real constraint: Cloudflare KV has no atomic compare-and-swap, so immutability is a single-writer read-before-write release rule rather than a KV-level atomic guarantee. | Independent `test:daily-edge-api`, private-buffer validation, elapsed-byte Gate, release-security scan, and full `npm test` PASS. Adversarial coverage includes 23:59:59.999Z→00:00:00.000Z switching, future rejection before/after cache priming, stale client clocks, missing active key with no yesterday fallback, malformed/oversized/KV-failure responses, full-chain unkeyed semantic forgery, wrong environment/key, partial-batch prevention, and backdated preparation rejection. The active ignored buffer contains 30 v2 manifests (`2026-08-25..2026-09-23`), receipt `b2f6f1eae6…3267e`; 24 dates from `2026-08-31` satisfy the real seven-day preload window, while the first six are intentionally ineligible. All 31 elapsed manifests remain byte-identical. | Cloudflare namespaces, bindings, encrypted credentials, external writes, and black-box edge behavior are not configured or observed. KV concurrency control still depends on the R7 single-writer workflow Gate. | R7 must preserve upload concurrency locking and compose the authenticated handler into Advanced Mode before any R8 binding/upload. Never backdate the first six private manifests. |
| R5 — Runtime state machine | PASS — 2026-08-24 | Implemented and independently reviewed `idle → loading → ready | unavailable`; replacement requests immediately clear/disable the old board, AbortController plus a monotonic token prevents stale response commits, Retry manages keyboard focus, and Daily/Infinite persistence is bound to `mode + puzzleId + contentHash`. Main review found corrupt terminal-state combinations that could lock a board or suppress a real win; the correction loop now accepts only consistent active/four-mistake/reveal/solve/crash-before-finish states and discards the rest without touching streak history. | Focused R5 Chrome and full runtime/full `npm test` PASS at 390×844. Adversarial coverage includes a transport resolving after abort, reordered requests, unavailable clicks/keyboard/New Infinite, API/hash failure with no fallback, valid recovery to the server UTC date, corrupt storage, legitimate terminal restoration, crash-before-finish completion, one-time win writes, rapid Infinite requests, failed Infinite retry on the same round, ARIA busy state, and focus recovery. Product regression preserves all protected tasks and 43 indexable routes. | Edge feature activation and field browser/network timing remain unobserved until authorized rollout. | No local R5 blocker. Keep the feature fail closed and do not enable it without R7/R8 binding and preload Gates. |
| R6 — Archive/SEO depth | PASS — 2026-08-24 | Added tracked elapsed-only public history, retained the latest-31 Archive view, added year/month discovery, generated exact dated content and adjacent navigation, and made sitemap/lastmod consume only immutable elapsed history. Main review rejected the first heading/canonical-only Gate; the correction binds every page to all 16 cards, four groups, copy/evidence and hashes, adds immutable `datePublished/dateModified`, removes a false stable-seed claim, derives Home/Archive/hub lastmods from history, rejects duplicate board/puzzle/content history, and makes an exact single next-date orphan recoverable after a manifest-write/index-rename crash while preserving conflicting artifacts fail closed. | Independent R6/static/full `npm test` PASS: 43 exact indexable routes = 9 evergreen + 31 dates + 1 year hub + 2 month hubs; real maximum Daily click depth is 2. Every date has unique title/description/H1, self canonical, Breadcrumb/WebApplication schema, manifest-exact payload/reveals, and elapsed-only adjacent navigation. Adversarial Gates reject stale lastmod, wrong canonical, copied page payload, fully rehashed thin board, conflicting orphan, future HTML/JSON/JS/worker/link leakage, and derivation material. Chrome 390×844/desktop navigation and protected product regression pass. | Post-deploy crawl, Google-selected canonical, indexed-route, and field CWV evidence remain unavailable. | No local R6 blocker. Keep tracked history append-only, preserve orphan recovery, and never source production builds from ignored future/private data. |
| R7 — Automation/security/release rehearsal | LOCAL PASS / EXTERNAL CONDITIONAL — 2026-08-24 | Composed the authenticated Daily handler into the Advanced Mode worker without losing redirects/ASSETS/404; added default-off worker/browser flags, HMAC envelopes, current-only elapsed publisher, readiness diagnostics, rollback drill, and three ordered automation jobs. The corrected security Gate explicitly covers seed, token, account/namespace IDs and preview/production HMAC inputs without accepting placeholder-looking literal secrets. A workflow contract now enforces least privilege, serialized non-cancelling jobs, explicit temporary manifests, no artifacts, narrow two-path commits, ordered fail-closed publication, and default-off edge activation. | Main and independent `npm test` PASS; next-day old-index→current append→release Gate→two-path commit rehearsal PASS; missing seed/HMAC/binding, wrong environment, tamper, empty buffer, duplicate/conflict, exact orphan recovery, KV/API failure, future public response, archive lag, idempotent no-op and rollback drills PASS; 202-candidate security scan, production build tamper Gate, audit, publication leak, regression, and Chrome 390×844 PASS. | Target Pages account `decab20e…` differs from authenticated Wrangler account `ce80f65f…`; preview/prod KV, binding, live API, cache, real UTC edge transition, account entitlement, quota and cost remain unverified. | Static/held rollout may proceed with both edge flags OFF. Do not create KV resources in the wrong account or claim atomic edge availability complete. |
| R8 — Authorized rollout | STATIC PRODUCTION PASS / EDGE BLOCKED — 2026-08-24 | User separately authorized commit, push, and deployment. Released the safe default-off worker/browser candidate, encrypted GitHub Daily seed, Daily release automation, Pokelike shadow automation, rebuilt question banks, elapsed Archive/SEO, and Today HOLD without enabling target-account KV routing or answer indexing. | Release commits `cae148d`, `ebfdcf2`, `2db65d1`, evidence commit `e85af38`, and final runtime/ledger commit `af7160d` reached `main`; Cloudflare deployment `84544784-e005-4749-93dc-649df569c313` PASS. Daily automation run `32697151219` passed all three jobs. Shadow run `32697656407` captured and committed exactly two evidence files after its path correction; evidence commit deployment PASS. Production black-box PASS: homepage 200/16 cards, Infinite 16 cards, Archive 31 cards, 43 sitemap routes, Today unavailable/noindex/no answer, Daily API default-off 404/no-store, true 404, legacy Infinite 308, Chrome 390×844 with zero page errors. | Target Pages account `decab20e…` still differs from authenticated Wrangler account `ce80f65f…`; KV namespaces/bindings/secrets, enabled edge API, preview, cache behavior, and real UTC edge transition are not verified. The 2026-08-24 manual Pokelike capture is a workflow preflight and does not replace the required 2026-08-25..2026-08-31 Asia/Shanghai 00:10 run or resolve republication permission. | Keep both edge flags and Pokelike Today publication/index flags OFF. Continue the scheduled 7-day evidence run; obtain target-account Cloudflare access before edge activation and resolve source permission before `VERIFIED→PUBLISHED→index`. |

### Controller acceptance rules

- A stage task's PASS is only a candidate result. Mainline acceptance requires reviewed diff plus rerun of its focused Gate and proportionate product, build, runtime, SEO, security, and baseline regression.
- Missing evidence blocks the affected claim or release; it never authorizes deleting routes, tools, content, navigation, facts, or user capabilities.
- Published Daily content is immutable. Future private content may be replaced only before activation with recorded hashes and without leaking it to public Git, logs, HTML, source maps, or endpoints.
- The controller does not advance a dependent phase while its prerequisite is conditional, unintegrated, or failed.

## Pokelike Today intent controller — started 2026-08-24

Objective: make `/pokelike-pokesort/today/` reliably complete the current Pokelike Daily Pokésort hint/answer task without conflating it with or reducing the independent 4×4 product.

Authoritative roadmap: `docs/roadmap/POKELIKE_TODAY_ROADMAP.md`, approved by the user in this Codex task.

### Protected baseline and ownership

- Baseline commit: `9ad6ea9` (`main`, aligned with `origin/main` at controller start).
- Concurrent ownership note: while QB1/QB2 were running, `main` advanced locally to user/other-task commit `87e5ea9` (`Fix search favicon assets and metadata`) and `docs/roadmap/POKELIKE_TODAY_ROADMAP.md` appeared. Both are preserved as outside this controller's stage ownership.
- Protected product: nine indexable routes, 31 playable generated 4×4 dates, three homepage sections, nine game buttons, Daily/Infinite/Archive, local progress/streak/share, hints/reveal/failure, mobile/keyboard behavior, Pokelike guide/worksheet, redirects, 404, and indexed surfaces.
- Preserved overlapping user work: the independent question-bank controller and its untracked `data/`, `scripts/puzzle/`, and roadmap files; GA4 injection/privacy/validation; favicon assets/declarations; and related existing modifications.
- Repository-local work is authorized. Commit, push, deployment, publication, Cloudflare/GSC mutation, paid services, and external communication are not authorized.

### Phase ledger

| Phase | Status | Reviewed/integrated scope | Verified evidence | Inferred / unverified | Blockers / rerun |
|---|---|---|---|---|---|
| PT0 — Baseline and ownership | CONDITIONAL PASS — 2026-08-24 | Protected surfaces and overlapping dirty-tree work recorded; authoritative roadmap added. Independent read-only review confirmed the question-bank, GA4, and favicon changes are separate protected work and identified high-overlap files requiring narrow patches. | `npm test` PASS; `node scripts/puzzle/test-pokemon-facts.mjs` PASS; baseline commit/status, 9 indexable routes, 31 dated routes, 3 homepage sections, 9 game buttons, 3 packs/12 groups/48 Pokémon, runtime tasks, and localStorage keys inspected; independent review complete. | Ownership of GA4/favicon is supported by the prior ledger rather than separate task metadata. Production does not evidence uncommitted work. | PT1 may perform read-only feasibility research. Today page/SEO publication work remains blocked until first-party data and reset semantics pass. |
| PT1 — Daily data feasibility | CONDITIONAL PASS — 2026-08-24 | Read-only investigation established a repeatable first-party extraction path from the official page/client, documented local-midnight reset behavior, verified two simultaneously live local-date puzzles, and recorded a hashed current state. Durable report: `docs/roadmap/POKELIKE_TODAY_FEASIBILITY.md`. Local contract and shadow tooling may proceed; public answer work may not. | Official HTML 200; versioned bundle `bundle.66f70857dc.js` SHA-256 `66f70857dc1427270ca9e7787ba914bb07c29974fdb2d2b60786d724f5b49668`; public `pcGeneratePuzzle`/`_pcState`; Asia/Shanghai+UTC #54 versus Los Angeles #53 at the same server time; #54 five relations true and unique count 1; independent read-only review. | No answer API; current bundle cannot reliably reconstruct historical live content; explicit republication permission, three natural resets, reset-edge CDN behavior, and seven-day shadow remain unverified. | PT2 and non-publishing shadow engineering permitted. Today page/index/publication remain blocked until natural-cycle and policy Gates pass. |
| PT2 — Contract and verifier | PASS — 2026-08-24 | Added isolated v1 manifest schema, relation verifier, canonical content hash, two first-party offline fixtures, freshness/publishability checks, and exhaustive rejection tests. Runtime supports all nine official relation families, validates each supplied link, enumerates all 720 orders, rejects unknown properties, and detects core-content tampering while retaining the observed first-party state hash. | Independent diff review; `npm run test:pokelike-manifest` PASS; full `npm test` PASS including parallel puzzle-data/QB2, 9 indexable routes, 31 dated routes, and Chrome 390×844 runtime; `git diff --check` has only pre-existing line-ending warnings. | Puzzle epoch is established from two consecutive simultaneously live first-party observations, not a published official contract. Natural-cycle/source-policy release blockers remain. | Preserve offline tests and do not weaken hash, uniqueness, freshness, or unknown-field rejection. |
| PT3 — Acquisition and state machine | CONDITIONAL PASS — 2026-08-24 | Added a non-publishing Playwright capture path, retained first-party HTML/bundle/state evidence, retry and two-sample consistency checks, explicit `PENDING`→`EXTRACTED`→`VERIFIED`/failure states, and a separately invoked fail-closed `PUBLISHED` transition. Publication now revalidates freshness, uniqueness, counters, transitions, retained samples, and reconstructed hashes both before and after the state change. Default capture is stdout-only; `--write` is explicit. | Live Asia/Shanghai dry-run captured official puzzle #54 twice with consistency hash `45323766e31e52f8d6ee73c7458f5514cae4b5a530a547c639e88e72519bebd7`, official/local solution counts 1, and status `VERIFIED`; `npm run test:pokelike-capture` PASS; `npm run test:pokelike-manifest` PASS; full `npm test` PASS including 9 indexable routes, 31 dated routes, and Chrome 390×844 runtime; independent implementation and repair review complete. | Seven natural-day observations, reset-edge CDN behavior, and explicit republication permission remain unverified. One successful same-day capture proves engineering behavior, not source stability or publication rights. | PT4 may implement a gated, noindex, non-publishing page/preview. No public answer claim, sitemap entry, or production publication until the seven-day shadow and source-policy Gates pass. |
| PT4 — Today task page | PASS — 2026-08-24 | Added `/pokelike-pokesort/today/` as a fail-closed task surface. The default build is visibly held/unavailable, contains no answer, is `noindex,follow`, and is absent from the sitemap. Ordinary answer rendering accepts only a current valid `PUBLISHED` manifest; an explicit environment-gated private preview may render `VERIFIED` data and remains noindex. Initial answer HTML includes date/reset/verification context, official CTA, progressive native disclosures, six ordered Pokémon, and five relation explanations. Browser-local date mismatch removes the answer instead of substituting another date. | `npm run test:pokelike-today` PASS for preview/published policy and stale/future/tampered rejection; 390px and desktop browser tests PASS for keyboard disclosure and timezone mismatch; full `npm test` PASS after concurrent QB4 integration, including static/SEO/regression and Chrome runtime. Default build independently confirmed zero answer positions and no sitemap entry. | No real `PUBLISHED` production record exists; only the private `VERIFIED` fixture path has rendered an answer. No-JS users receive an explicit date-check warning because client-local timezone detection requires JavaScript. | Public/index release remains blocked by PT1/PT3 natural-cycle and source-policy Gates. PT5 may refine non-overlapping metadata/routing while keeping Today held. |
| PT5 — SEO and routing | PASS — 2026-08-24 | Separated daily answer/hint intent at Today, evergreen six-Pokémon rules/worksheet intent at the guide, and the independent 4×4 Daily/Archive/Infinite cluster. Today has distinct answer-intent title/H1/description/OG/self-canonical WebPage schema while remaining held/noindex and absent from the sitemap. The guide links to availability with an explicit publication-held label; 4×4 metadata, navigation, H1s, and generated date pages identify their different task. Added a dedicated intent-routing validator. | `npm run validate:pokelike-intent` PASS; `npm run test:pokelike-today` PASS; full `npm test` PASS; validator retained all nine protected routes within the concurrently expanded 40-route indexable set and confirmed Today held/noindex/no-answer/sitemap exclusion; Chrome 390×844 regression PASS. | Search-engine interpretation and Query × Page behavior remain field evidence, not locally provable. Public Today metadata has not been crawled because release remains held. | Do not add Today to sitemap or remove noindex until PT1/PT3/PT7 release Gates pass and publication is separately authorized. |
| PT6 — Measurement and monitoring | CONDITIONAL PASS — 2026-08-24 | Added one allowlisted Today analytics entrypoint with six low-cardinality events; event contracts exclude Pokémon/order, relation and worksheet values, saved state, free text, query strings, user IDs, and localStorage. GA configuration uses origin+pathname only and remains exactly once per HTML page or zero when disabled. Privacy copy matches behavior. Added a repo-local readiness monitor that reconstructs retained evidence, anchors seven dates to the expected Asia/Shanghai local date, enforces a 26-hour freshness ceiling, deduplicates dates, and requires valid acquisition/stale failure drills. | `npm run test:pokelike-analytics` PASS including forbidden-field and fake/tampered/historical/stale/duplicate evidence rejection; default GA build has one loader/config and disabled build has zero; `npm run monitor:pokelike-readiness` honestly reports zero records and `BLOCKED`; full `npm test` PASS with 40 indexable routes and Chrome runtime. The GA4 skill's static multi-page integration rules informed the single-load and page-location implementation. | Observed GA4 events, task-completion rates, GSC Query × Page, and field performance are `UNVERIFIED`; no external analytics access was authorized or performed. | PT7 must remain blocked until seven current evidence-backed shadow days and valid failure drills exist. External analytics evidence is a release follow-up unless separately authorized. |
| PT7 — Shadow and release readiness | BLOCKED — 2026-08-24 | Completed local release audit and added an isolated deterministic failure-drill harness. Simulated acquisition/stale drills are clearly marked, hashed, stored separately under `data/pokelike/drills/`, and cannot contribute to official observation counts. Default Today remains held/noindex/no-answer and the product baseline is preserved. | Full `npm test` PASS; Chrome 390×844 plus desktop Today review PASS; native disclosure keyboard semantics PASS; `npm audit --audit-level=low` reports 0 vulnerabilities; 3 homepage sections, 9 controls, all core tasks, 9 protected indexable routes and the concurrent 40-route expanded set preserved; two validated simulated drills pass. | External GA4/GSC/field evidence remains unverified. First-party republication permission remains unresolved. The parallel favicon commit that advanced `HEAD` is outside this controller and contains no Today implementation. | Readiness monitor reports `recordCount: 0`, `consecutiveVerifiedDays: 0/7`, freshness false, and `localGate: BLOCKED`. Capture one real Asia/Shanghai observation per natural day for seven current days; simulated drills never satisfy this. Do not publish/index/deploy Today. |
| PT8 — Release handoff | PASS — 2026-08-24 | Added `docs/roadmap/POKELIKE_TODAY_RELEASE_HANDOFF.md` with the blocked evidence state, fixed seven-day Asia/Shanghai capture schedule, immutable evidence review, drills/tests/audits, authorization checkpoints, verified-to-published transition, held/preview/production build modes, future index-policy diff, preflight matrix, Cloudflare Pages Git deployment, production verification, fail-closed rollback, and separately authorized GSC/GA4 follow-up. | Referenced repository paths and npm scripts exist; Markdown and `git diff --check` pass apart from existing line-ending notices; monitor rerun confirms 0/7 and `BLOCKED`. No app behavior or external system was changed in PT8. | Deployment configuration, published record, indexing code, production behavior, GSC/GA4 evidence, and source-policy approval remain unverified. | Overall Today release remains BLOCKED. No commit, push, deploy, publication, Cloudflare/GSC/GA4 mutation, or external message is authorized by this handoff. |

## Question-bank rebuild controller — started 2026-08-24

Objective: replace the three-pack modulo loop with a source-backed, validated puzzle system that can publish immutable Daily boards and a materially large Infinite pool without reducing existing routes, controls, content, or SEO safeguards.

Authoritative roadmap: the six-stage question-bank roadmap approved by the user in this Codex task after the `pokesort.com` implementation review.

### Protected baseline for this controller

- Baseline commit: `9ad6ea9` (`main`, aligned with `origin/main` at controller start).
- Existing user-owned working-tree changes are preserved: GA4 injection/validation, favicon assets and declarations, related page/privacy copy, and icon generation changes.
- Nine current indexable routes, 31 playable dated routes, all Daily/Infinite/Archive tasks, local progress/streak/share behavior, mobile controls, Pokelike worksheet, redirects, 404 behavior, and current SEO documentation are protected.
- The twelve existing authored groups remain useful content and test fixtures; replacing the three-pack selection mechanism does not authorize deleting them.

### External-action limits

This approval authorizes repository-local implementation, tests, task coordination, and integration. It does not authorize push, production deployment, Cloudflare resource creation, GSC mutation, paid services, or external communication.

### Phase ledger

| Phase | Status | Reviewed/integrated scope | Verified evidence | Inferred / unverified | Blockers / rerun |
|---|---|---|---|---|---|
| QB0 — Baseline and ownership | PASS — 2026-08-24 | Inspected current source, prior ledger, active/completed tasks, dirty-tree ownership, runtime selection, build/SEO validators, and deployment automation. Confirmed the fault: Daily and Infinite both select `GROUPS[hash(seed) % GROUPS.length]`; 31 dates expose only three semantic boards. | `npm test` PASS on the preserved dirty tree; static/SEO/regression and Chrome runtime Gates pass; baseline commit and user-owned diff recorded above. | Production remains on the last pushed commit and is not evidence for this uncommitted controller work. | Preserve the GA4/favicon diff and do not commit, push, or deploy without separate authorization. |
| QB1 — Facts and category model | PASS — 2026-08-24 | Added a pinned, reproducible National Dex 1–1025 snapshot; category rules and offline enumeration for type, exact dual type, generation, color, topology-based evolution stage, baby, legendary, and mythical; explicit refresh script; integrated the focused Gate into `npm test`. Existing alternate-form and community classifications remain outside v1 rather than inferred. | PokeAPI commit `f15414790832c88d784d1537658b957fd73cbbbd`; seven source CSV URLs and SHA-256 values; 1,025 continuous IDs across Gen I–IX; deterministic refresh SHA-256 `EEB8E71E748E8581D2C0C8569523672349D28DBC1A8706295FFF67337DE9F500`; focused data test PASS; mainline `npm test` and `git diff --check` PASS. | Evolution stage is a declared deterministic topology label, not an official editorial classification. Alternate forms, moves, abilities, evolution methods, starters, fossils, Ultra Beasts, and community conventions are unverified/out of v1 scope. | No QB1 blocker. Refresh is explicit and networked; ordinary build/test consumes only the committed snapshot. |
| QB2 — Generator and uniqueness solver | PASS — 2026-08-24 | Added deterministic PRNG/canonical hashing, 100-instance rule universe, 16-bit quartet/exact-cover solver, bounded generator, typed failure, human-facing evidence copy, and batch audit/duplicate controls. Main review removed unsupported “official” wording from PokeAPI flag hints and integrated focused/stress scripts into package commands. | Ambiguous and broad-rule fixtures PASS; same-seed byte stability PASS; `npm run test:puzzle-stress` equivalent 1,000-board run produced 1,000 unique board signatures and 4,000 unique exact group signatures with zero duplicates/exhaustion; mainline `npm test` and `git diff --check` PASS after review. | Constructive v1 anchors prefer exact dual type plus PokeAPI baby/mythical flags; the full 100-rule universe remains the solver Gate. Daily cooldown, route integration, and browser use remain unverified until QB3/QB4. | No QB2 blocker. Keep all candidate loops bounded and never fall back to an old puzzle after exhaustion. |
| QB3 — 365-day immutable Daily calendar | PASS — 2026-08-24 | Added and main-reviewed an immutable 2026-07-25..2027-07-24 calendar, strict validator, byte-stability/tamper/gap tests, build-time manifest loading, per-page browser payloads, Archive exact-content rendering, and puzzle-ID-bound storage reset. Homepage and 31 published-date routes no longer use the three-pack modulo selector; the authored packs remain only as protected legacy fixtures and temporary Infinite input pending QB4. Dated indexing remains held for the full release Gate. | 365 distinct puzzle IDs/boards; 1,460 distinct exact/member groups; 14-day achieved predicate cooldown; zero consecutive-day species repeats; complete 100-rule solver reproved 365/365 unique; 31 publishable and 334 future-stored as of 2026-08-24; immutable overwrite, tamper, gap, and byte-stable rerun tests PASS; mainline `npm test` including Chrome 390×844 PASS. | Future manifests are stored but neither published nor indexed. Calendar v1 ends 2027-07-24 and requires an explicit versioned extension before that date. Infinite still uses legacy packs until QB4. | No QB3 blocker. Preserve the no-fallback build failure when today's immutable manifest is unavailable. |
| QB4 — Infinite pool/runtime | PASS — 2026-08-24 | Added an immutable 1,000-board Infinite pool in 20 lazy-loaded shards, a full validator/test Gate, a deterministic full-pool permutation, Daily board/group exclusions, runtime fetch/error handling, puzzle-ID-bound storage, and truthful finite-pool copy. Removed the runtime dependency on the legacy three-pack selector; unavailable verified data now fails visibly without substituting an old board. | 1,000 distinct boards, 4,000 distinct exact/member groups, all 365 Daily boards and 1,460 Daily groups excluded; complete solver reproved 1,000/1,000 unique; 1,000-round permutation and first-500 uniqueness PASS; byte-stable rerun, tamper detection, immutable overwrite block PASS; emitted 20 shards byte-match source; Chrome loads every shard, solves Infinite #1, loads a different #2, and confirms 500 unique runtime selections. | The finite pool intentionally cycles after all 1,000 boards; it is not described as mathematically endless. Browser caches only requested shards. | No QB4 blocker. Cloudflare dynamic generation is unnecessary for v1 and no external resource was created. |
| QB5 — SEO, product regression, and release readiness | LOCAL PASS — 2026-08-24 | Released the materially unique dated-page policy locally: Archive UI remains latest 31 while every elapsed immutable manifest keeps a persistent indexable route; future manifests remain absent. Added real board profiles, exact Daily payloads, progressive hints/four reveals, solver overlap evidence, self canonicals, Breadcrumb schema, date `lastmod`, and updated keyword/IA/implementation maps. Corrected Categories, How-to, Home, About, and Alternative copy to the actual pinned 100-predicate model. Added Infinite lazy-load/race/fail-closed behavior and retained all protected routes, homepage sections, controls, navigation, GA4, Pokelike guide/Today HOLD, redirects, 404, and responsive interactions. | Final `npm test` PASS: 40 canonical indexable routes (9 protected + 31 published Daily), no future routes, full static/intent/product Gates, Chrome 390×844, Daily and Infinite solve/share/storage/failure/reveal, 500 runtime selections, rapid Infinite requests, Pokelike tests, and true 404. `npm audit --audit-level=low`: 0 vulnerabilities. `git diff --check`: PASS with line-ending notices only. Protected homepage remains 3 sections/9 controls; Archive remains 31 cards; routes/indexed surfaces expand without deletion. | Search demand/volume and post-deploy Google-selected canonical/CWV evidence remain unavailable. The finite Infinite pool cycles after 1,000. Daily calendar extension is required before 2027-07-24. | Repository candidate is locally release-ready. Commit, push, production deployment, Cloudflare mutation, and GSC actions remain outside this controller approval. |

Question-bank controller outcome: **COMPLETE — LOCAL RELEASE CANDIDATE**. External release actions require separate authorization.

Controller started: 2026-08-23 (Asia/Shanghai)

Authoritative roadmap: the eight-phase roadmap approved by the user in this Codex task. This ledger is updated only after main-worktree verification.

## Protected product baseline

Baseline commit: `2150c42` (`main`, aligned with `origin/main` at controller start).

- Six original public source pages: `/`, `/archive/`, `/how-to-play/`, `/pokesort-alternative/`, `/pokesort-down/`, `/privacy/`.
- Homepage keeps the playable Daily board before supporting content.
- Daily, Infinite query-state, and Archive selection tasks remain available.
- Twelve authored groups arranged as three complete four-by-four puzzle packs.
- Local progress, Infinite round, solved groups, mistakes, Daily wins, and streak state remain browser-local.
- Shuffle, Hint, Reveal, Submit, Deselect, Share, New Infinite, touch/click, keyboard arrows, focus state, and reduced-motion behavior are protected.
- Main navigation, responsive four-column board, visible information depth, and existing indexed surfaces must not be reduced without an explicit product decision.

## External-action limits

The original approval authorized repository-local implementation, review, tests, and integration. On 2026-08-24 the user separately authorized commit, push, and deployment after code/logic and SEO review passed. GSC mutation, paid services, unrelated DNS changes, and external messages remain outside scope.

## Phase ledger

| Phase | Status | Reviewed/integrated scope | Verified evidence | Inferred / unverified | Blockers / rerun |
|---|---|---|---|---|---|
| 0 — Baseline and change ownership | PASS — 2026-08-23 | Independent review found and drove repair of an initial 30→2 Archive regression. Final candidate preserves today plus 30 prior deterministic boards, legacy `?date=` play, all original routes/tasks, and all game data. The later Phase 4 publication policy preserves every date route while holding all dated pages at `noindex,follow` until packs are unique. | `npm test`; 31 Archive cards/routes; puzzle data exact-match at 3 packs/12 groups/48 Pokémon; homepage remains 3 sections/9 buttons; six original pages and all handlers/localStorage markers preserved; independent re-review | Browser interaction and deployed behavior intentionally deferred to later Gates | No Phase 0 blocker; do not reintroduce Archive shrinkage |
| 1 — Search intent and opportunities | CORRECTED PASS — 2026-08-23 | The first review correctly detected a 4×4/6-link mechanic mismatch but incorrectly converted it into blanket Pokelike exclusion. Corrective research established that Pokésort is the Daily six-Pokémon minigame inside Pokelike. The map now targets evergreen entity/rules/tips intent at `/pokelike-pokesort/`, holds one future verified Today URL and a complete solver, and keeps 4×4 tasks on their own pages. | Official Pokelike product copy; current Pokelike guide/FAQ; r/Pokelike policy, tips, and dated hint threads; public working solver; corrected CSV and opportunity scores; independent correction review | GSC volume/Query × Page data remains unavailable; daily answer data is not yet ingested or independently verified | Monitor the new guide after authorized release; do not publish `/today/` until six names, five links, order, date, and reset boundary are auditable; do not call the worksheet a solver |
| 2 — Crawl and index foundations | PASS — 2026-08-24 | Production now serves clean apex canonicals/OG URLs, 9 sitemap routes including the distinct Pokelike guide, 31 playable but held date routes, robots, explicit noindex policy, and a truthful 404. | Local `npm test` plus external production matrix: HTTP→HTTPS 301; www→apex 308; legacy Infinite and in-window date 308; 9 routes 200/self-canonical/one H1/no placeholder; unknown and future date real 404; sitemap clean; social asset 200; dated page self-canonical and `noindex,follow` | Cloudflare injects managed Content-Signal directives before the generated robots policy; search remains allowed and the apex sitemap declaration is preserved | Monitor Google-selected canonicals and coverage in GSC when separately authorized |
| 3 — Daily and Infinite separation | PASS — 2026-08-23 | Repaired reveal/win boundaries, four-mistake lockout, canonical mode navigation, mode-specific completion/share, New Infinite visibility, accessible mistake count and mode state, and focus restoration | Real headless Chrome at 390×844: 16-card Daily/Infinite boards; correct group, One Away, hint, Arrow/focus, valid Daily win/streak/share; Reveal no win; four failures no win and 16 disabled cards; Infinite navigation/solve/share; dated board render; zero page errors. `npm test` and independent final re-review PASS | Visual screenshot review and field INP remain deferred; code/browser interaction Gate is complete | Preserve localStorage key families and never allow Reveal/failure to create Daily wins |
| 4 — Archive and dated pages | CONDITIONAL PASS — 2026-08-24 | Preserved a static, month-grouped 31-date Archive with exact content, navigation, breadcrumbs, progressive hints, and `noindex,follow` publication hold. The edge redirect window is pinned to the actual build artifact. Added a GitHub schedule at 00:05 UTC that runs the static Gate and pushes a dated refresh commit to trigger the existing Cloudflare Pages Git deployment. | `npm test`; 31 exact cards/routes; production in-window query 308 to an existing static route; outside-window query 200 compatibility; newest+1/oldest−1 and invalid date edge regressions; production Chrome Archive count 31; two manual workflow dispatches PASS; bot refresh commits `0bf21dd` and `ce2fee5`; both downstream Cloudflare Pages checks succeeded | GSC evidence for an index release is unavailable. GitHub schedule timing can occasionally be delayed by the hosted runner queue, but the refresh chain is operational. | Monitor scheduled runs; keep dated index release held until packs are materially unique |
| 5 — Core content and trust | PASS — 2026-08-23 | Preserved every content surface while clarifying 4×4 intent and actual mechanics. Added third-party sprite privacy disclosure and a 12-group fact-evidence register; marked pseudo-legendary as a community convention; retained Alternative at `noindex,follow` and troubleshooting as evergreen rather than live status. | `npm test`; all evidence rows match exact pack/group/four-member sets; validator enforces parity; privacy matches `raw.githubusercontent.com` behavior; How-to title/H1 identify 4×4; no analytics/cookie/beacon code found; trademark/non-affiliation and three-pack limitations visible; independent final re-review PASS | Sources include structured PokeAPI data and community references, not an official data license; deployment-platform integrations remain unknown until release | Re-review the register and privacy disclosure whenever packs, image hosting, analytics, ads, or external services change |
| 6 — Automated SEO and regression | PASS — 2026-08-24 | Expanded `npm test` into static SEO, protected-product/data, real-Chrome runtime, corrupted-storage, legacy-share, strict-date, and build-pinned edge Gates. | Static + Playwright 1.62.1 Chrome at 390×844 pass; independent code/logic PASS; production Chrome smoke passes Daily, Infinite, Archive, worksheet, and real 404; npm audit reports 0 vulnerabilities | Field CWV remains unavailable; local runtime requires an installed Chrome channel or configured executable | Rerun full `npm test` on every change and perform the external matrix after every production deployment |
| 7 — Release readiness and GSC handoff | PRODUCTION PASS — 2026-08-24; MONITORING PENDING | Commit `0bcc3db` was pushed to `main`; Cloudflare Pages check completed successfully and the candidate is live at `https://pokesort.org`. Product baseline is preserved and expanded. | Git push `2150c42..0bcc3db`; Cloudflare Pages `completed/success`; external HTTP matrix; 9 self-canonical production routes; clean sitemap; production Chrome smoke; independent code/logic and Technical SEO PASS | GSC Query × Page, Google-selected canonical, field CWV, and automatic daily UTC rebuild are not configured/available | No deployment blocker remains. Separately authorize GSC actions if desired; rebuild after UTC midnight for a fresh Archive window |

## Commit and integration policy

Commit, push, and deployment were authorized on 2026-08-24 after final review. Existing user work will not be reset, cleaned, or overwritten; GSC submission remains a separate follow-up action.

## Production release record — 2026-08-24

- Release commit: `0bcc3db` (`main`).
- Cloudflare Pages Git check: `completed / success`.
- Production: `https://pokesort.org`.
- Verified redirects: HTTP→HTTPS `301`; www→apex `308`; `?mode=infinite`→`/infinite/` `308`; generated-window date query→static date route `308`.
- Verified compatibility/status: outside-window date query `200`; unknown and future date routes `404`; all nine sitemap routes `200` and self-canonical.
- Verified browser tasks at 390×844: 16-card Daily selection, 16-card Infinite navigation, 31-card Archive, six-position/five-link Pokelike worksheet, and real 404.
- Daily refresh automation: `.github/workflows/daily-archive-refresh.yml` at 00:05 UTC; manual runs `32676815635` and `32676917342` passed; refresh commits triggered successful Cloudflare Pages deployments. Action runtimes were upgraded to `checkout@v5` and `setup-node@v5` after the first-run deprecation warning.
